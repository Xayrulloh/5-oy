## pagination input
input Pagination {
    page: Int
    limit: Int
}

input Sort {
    byDate: SortOptions
    byName: SortOptions
}

input FoodSort {
    byDate: SortOptions
    byPrice: SortOptions
    byName: SortOptions
}

## enum types
enum SortOptions {
    toLargest
    toSmallest
}